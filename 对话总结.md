# 高校通讯录（移动端）· 开发记录与对话总结

> 本文档汇总本会话中从需求到各次迭代的全部内容，便于备份、交接与回顾。
> 项目：杭州职业技术大学 移动端通讯录（部门 / 成员三级导航）。

---

## 一、项目概览

| 项 | 说明 |
| --- | --- |
| 形态 | 纯前端单页应用（原生 HTML/CSS/JS，无框架） |
| 定位 | 移动端优先，桌面以 max-width 420px 手机模拟外框居中预览 |
| 主色 | 蓝 `#1677ff`（C 端约定：股票涨红跌绿不在此列，本项目用蓝色主色） |
| 导航 | 三级页面栈：部门列表 → 成员列表 → 成员详情，支持右进左出滑动 |
| 数据 | 内置 Excel 全量导入种子数据（785 人 / 32 部门，0 孤儿数据） |
| 持久化 | localStorage（键 `hzvtc_contacts_v2`） |
| 离线 | PWA（manifest + Service Worker，网络优先策略） |
| 导入导出 | SheetJS（本地 `xlsx.full.min.js`，避免国内 CDN 不稳定） |

**核心文件**
- `index.html` — 页面骨架、状态栏、屏幕容器、模态层、隐藏文件选择器
- `styles.css` — 设计令牌（颜色 / 间距 / 字号）、全部页面与组件样式
- `app.js` — 全部逻辑：导航栈、搜索、Excel 导入导出、拖拽排序、拨号确认、双击返回
- `seed-data.js` — `window.SEED_DATA`（由用户 31 张工作表 Excel 解析生成）
- `sw.js` — Service Worker（缓存 v3，网络优先）
- `manifest.webmanifest` — PWA 清单
- `xlsx.full.min.js` — 本地 SheetJS
- `dist/` — 上述文件的部署副本（用于 CloudStudio 发布）

---

## 二、需求演进（按轮次）

### R1 — 基础架构
- 移动端通讯录，DingTalk/Feishu 风格，白底蓝主色。
- 三级导航：部门 → 成员 → 详情；页面滑动切换。
- 点击号码 `tel:` 拨号；支持 Excel 导入、增删改查、localStorage 持久化。
- 指定颜色、字体等设计令牌。

### R2 — 组织架构管理
- 支持**拖拽**自由调整部门顺序，以及同一部门内成员顺序；调整后立即保存。
- 点击电话号码先弹**确认对话框**，确认后再拨出。

### R3 — 精简字段
- 去除【添加子部门】按钮。
- 人员信息中去除【邮箱】字段。
- 提供手机端使用说明。

### R4 — Safari 兼容 + Excel 批量导入
- 修复 Safari 中底部「导入 Excel」按钮无法点击（布局 / 定位 / 跨浏览器）。
- 实现按用户指定 Excel 格式**批量解析并校验**导入，可重复执行。
- 解释华为 Mate60 打开慢的原因（本地 CDN / 资源体积）。

### R5 — 全量覆盖 + 调整顺序按钮 + 离线调研
- 导入 = **完全覆盖**（清空全部，仅保留 Excel 数据）。
- 两处【添加人员】右侧新增【调整顺序】按钮；默认隐藏拖拽手柄，点击后显示并启用拖拽。
- 提出离线方案（最终确认采用方案 A：PWA）。

### R6 — 采用 PWA 离线
- 将 Excel 全量数据作为系统默认种子导入。
- 实现并展示导入结果（清晰的列表）。

### R7 — 搜索与展示优化
- 顶部搜索框支持按**姓名 / 部门 / 职务 / 座机号 / 手机号**实时模糊搜索。
- 移除电话旁的聊天 / 短信入口（message 图标）。
- 成员头像改为**完整显示姓名**。

### R8 — 缓存导致的旧版本问题
- 用户反映打开网址仍是旧版本。
- 修复：Service Worker 由 cache-first 改为 **network-first**（静态资源与导航均网络优先），缓存版本 v1→v3，并增加 `controllerchange` 自动刷新。

### R9 — 搜索体验细化
- 搜索结果**不按匹配度排序**（保持原顺序）。
- 点击搜索框显示**历史记录**、隐藏联系人；输入时隐藏历史、显示实时结果。
- 实时匹配文字以**红色高亮**。

### R10 — 部门页全局搜索（搜人优先）
- 部门列表搜索框升级为**全局员工搜索**：输入姓名 / 座机 / 手机，直接定位到**个人详情**。
- 仅当无员工匹配时，回退到部门名匹配。
- 唯一匹配 0.7s 后自动跳转其详情页。

### R11 — 双击空白处返回（本会话新增）
- 在页面非交互（空白）区域检测**快速双击**，执行返回上一级（`popPage`）。
- 通过位移 / 时长阈值区分点按与滚动、拖拽、长按；多指触控与搜索聚焦时禁用，误触率低。
- 首次启动只提示一次操作方式。

### R12 — 拨号确认按钮显示异常修复（本会话新增）
- 现象：确认弹窗蓝色按钮被巨大电话图标撑满、文字被遮挡。
- 根因：按钮内嵌的 SVG 无固定宽高，在 `.btn` 中拉伸成超大图形。
- 修复：去掉按钮图标，仅保留文字「拨打」；`.btn` 增加 flex 居中 + 文字不换行；并统一 `.btn svg / .sheet-item svg` 为 18×18。

### R13 — iOS 风格设计改版（2026-08-08 维护）
- 设计系统整体重写为 iOS 风格（参考 Apple HIG / Designing Fluid Interfaces）：
  - 主色换为 iOS 系统蓝 `#007AFF`（深色 `#0A84FF`），分组列表灰底 `#F2F2F7`，文字层级用系统黑/灰阶，分隔线改 hairline 半透明。
  - 列表改为 iOS 分组圆角卡片（`#list` 内按区段包 `.card`），区段标题灰色小字；部门/成员行、搜索历史、详情信息卡同步重绘。
  - 顶栏 / 状态栏 / 弹窗改为毛玻璃（`backdrop-filter: blur + saturate`）；底部 Sheet 增加抓取条、圆角与弹性过渡（iOS 曲线 `cubic-bezier(.32,.72,0,1)`）。
  - 页面滑动转场保持右进左出 + 视差，换用 iOS 曲线；`popPage` 清理时机同步为 470ms。
  - 头像换 iOS 通讯录色板；三字姓名头像字号单独收紧（`[data-len="3"]`）。
  - 大标题 28px 负字距；按钮、搜索框、选择器、toast 全部 iOS 化。
  - 新增深色模式（`prefers-color-scheme`）、减少动态（`prefers-reduced-motion`）、减少透明度（`prefers-reduced-transparency`）适配。
  - 新增触觉反馈 `haptic()`（`navigator.vibrate`，仅 Android 生效）：解锁、增删改、排序、导入、拨号确认等关键提交。
  - 更新 manifest 主题色 / 图标、新增 favicon；SW 缓存版本 v3 → v4。

### R14 — 口令锁（CastASpell，2026-08-08 维护）
- 启动时先显示 iOS 风格锁屏（应用图标 + 名称 + 密码框 + 解锁按钮），输入正确口令后进入应用。
- 口令 `CastASpell`，区分大小写；错误时输入框红边 + 卡片抖动 + toast「口令错误，请重试」。
- 解锁状态原写入 `sessionStorage`（键 `hzvtc_unlocked_v1`）：同一次浏览器会话内保持解锁，关闭标签页后重新要求口令（**R21 已改为设备级持久化**）。
- 锁屏页在 `#phone` 内、状态栏之下，`hidden` 属性 + CSS 显式覆盖保证正确显隐。

### R17 — 数据加密与仓库重建（2026-08-08 维护）
- 隐私升级（方案 A）：种子数据（785 人）用口令经 **PBKDF2-SHA256(100000) → AES-256-GCM** 加密后发布。
  - 明文种子保存在 `seed-data.private.js`（**已被 git 忽略**，不进仓库）；加密脚本 `encrypt-seed.cjs` 生成 `window.SEED_DATA_ENCRYPTED` 到 `seed-data.js` 与 `dist/seed-data.js`。
  - **口令本身不写入代码**：锁屏用「能否成功解密」校验口令，app.js 中不再有口令明文。
  - 解锁成功才解密并写入 localStorage（存于用户自己手机）；首次解密后刷新不再重复解密。
- 仓库重建：删除并重建 `SirTy998/hzvtc-contacts`（授权补 `delete_repo` 权限），旧提交中的明文数据随之彻底清除（旧 SHA 原始文件已 404）；网站仅发布 `dist/`，网址不变。
- 说明：加密可挡住抓取数据文件/爬虫等常规泄露；口令即钥匙，口令泄露等于数据泄露。静态应用无法抵御刻意逆向，如需更高防护需服务器级访问控制（方案 C）。

### R15 — 细节修复（2026-08-08 维护）
- 移除锁屏「口令区分大小写」提示行（`.lock-hint` 不再使用，CSS 保留待用）。
- 修复根目录部门页无法拖拽排序：根页排序组此前直接挂在 `#list` 容器上，拖动时行会被移出 `.card` 卡片导致视觉与交互异常；改为排序组绑定卡片容器 `#group-root-depts`（与成员页的 `#group-depts / #group-members` 一致）。已用触摸事件模拟验证：拖动生效、顺序保存、行保持在卡片内。

### R16 — GitHub Pages 固定网址上线（2026-08-08 维护）
- 为「更新后网址不变」选择 GitHub Pages 托管：仓库 `SirTy998/hzvtc-contacts`（公开），网址：
  **https://sirty998.github.io/hzvtc-contacts/**
- 采用 GitHub Actions 自动部署（`.github/workflows/pages.yml`）：每次向 `main` 推送，自动把 `dist/` 内容发布到 Pages，网址不变。
- 已端到端验证：锁屏出现、错误口令拦截、正确口令 `CastASpell` 解锁、31 个部门渲染、刷新后会话保持。
- 说明：仓库公开，仅发布 `dist/` 内容到网站（源码与文档在仓库中可被查看，但不在网站目录）。

### R18 — 排序退出修复（2026-08-08 维护）
- 现象：排序模式下拖拽多次后，点「完成」要点很多次才能退出。
- 根因：每次拖拽结束都注册一个「拦截下一次点击」的全局监听（`blockNextClick`），多个监听堆叠后每次真实点击被吞掉一个（与拖拽次数成正比）。
- 修复：改为拖拽结束后 400ms 内仅忽略列表行/拨号/面包屑的合成点击（`isDragSyntheticClick`），按钮（含「完成」）点击始终放行。已用触摸模拟复现并验证：拖拽 2 次后点 1 次「完成」即退出。

### R19 — 机构与中层干部数据更新（2026-08-21 维护）
- 数据源：`0722更新机构及中层干部一览表（添加号码）(1)(1)(1).xlsx`（校领导 11 人 + 机构 41 个 + 部门领导 96 人）。
- 本次更新为**全量覆盖**：部门调整为 Excel 新机构（26 个机关/直属机构 + 15 个二级学院），成员仅保留 Excel 中已确认的校领导与部门领导（共 107 人）；各部门内部人员待确认后再补。
- 旧明文种子已备份为 `seed-data.private.backup-2026-08-21.js`（不入库），旧加密版本保留在 git 历史中。
- 已用 `node encrypt-seed.cjs CastASpell` 重新加密并同步 `dist/seed-data.js`；WebCrypto 模拟验证：正确口令解锁 43 部门 / 107 人，错误口令拒绝。
- 注意：手机端若仍开着旧页面，需完全关闭标签页后重新打开并解锁，才会拿到新数据（旧 localStorage 不会自动被新种子覆盖）。

### R20 — 双击空白返回误触修复（2026-08-21 维护）
- 现象：快速双击页面空白处时，会“弹回上一界面又马上弹入下一界面”，无法成功返回。
- 根因：双击触发 `popPage()` 后，浏览器仍会为同一手势派发残余 `click`；此时页面栈已变化，该 click 落在上一页同坐标的交互元素上（部门行/成员行/返回键等），点击处理随即再次导航，甚至连锁推进两层。
- 修复：新增 `lastDblTapPop` + `isDblTapSyntheticClick()`（500ms 守卫窗口，沿用拖拽排序的 `lastDragEnd` 抑制模式）：双击返回后短时间内忽略一切 `data-act` 点击；守卫过期后正常点击恢复。
- 验证：Playwright + 无头 Chrome（touch 模拟）回归测试——修复前同场景点击落入部门行并推进两级（FAIL），修复后双击正常返回且守卫内点击被抑制、过期后点击恢复（PASS）。

### R21 — 解锁状态设备级持久化（2026-08-21 维护）
- 需求：固定在桌面（PWA）后点击进入仍需输入口令；期望同一设备登录过一次后不再要求。
- 变更：解锁标记由 `sessionStorage` 改为 `localStorage`（键不变 `hzvtc_unlocked_v1`），同一设备/浏览器解锁一次后，后续打开（含 PWA 桌面图标）直接进入。
- 安全兜底：`isUnlocked()` 同时校验本地通讯录数据存在（`hzvtc_contacts_v2`）；若用户清除站点数据，解锁标记与数据一并清除，会重新要求口令。
- 验证：Playwright 场景——全新浏览器上下文先出现锁屏，解锁后关闭页面，再开新页面（同上下文）直接进入且数据完整。

## 六、维护说明（2026-08-08 起）
- 源码维护目录：`/Users/ty/Desktop/高校通讯录`（不再依赖 WorkBuddy）。
- 修改后需同步 `dist/` 部署副本（`cp index.html styles.css app.js manifest.webmanifest sw.js dist/`，种子数据与 SheetJS 不变时无需重复拷贝）。
- **日常更新流程（GitHub Pages）**：改源码 → 同步 `dist/` → `git add -A && git commit -m "说明" && git push` → Actions 自动部署，网址不变。
- **数据更新流程**：拿到新 Excel → 用现有导入逻辑生成 `seed-data.private.js`（明文，本地保留）→ `node encrypt-seed.cjs 口令` 重新加密 → 同步 `dist/` → 推送，网址不变。
- 旧 CloudStudio 链接仍在线（旧版本）；如需继续使用 CloudStudio，重新发布 `dist/` 会生成新沙箱链接，旧链接失效。

---

## 三、关键技术点

### 1. 导航栈（页面进出）
- `#screen` 持久容器承载多个 `.page`（绝对定位、translateX 切换）。
- `pushPage / popPage` 管理 `stack` 数组；根页面（`stack.length<=1`）时 `popPage` 自动忽略，避免「退出应用」。

### 2. 拖拽排序（Pointer Events）
- 自定义指针拖拽引擎：克隆 + 占位符，拖拽结束后用 `blockNextClick` 在捕获阶段拦截紧跟的 click，避免误触发导航。
- 顺序字段 `sortOrder` + `byOrder / childDepts / deptMembers / nextOrder / normalizeOrders`；调整后立即 `saveData()`。

### 3. Excel 导入（多 Sheet + 模糊表头）
- `mapColumns / findHeaderRow / normalizeName / normalizePhone / ensureDept / upsertMember`。
- 全量覆盖：清空（保留 root）→ 按工作表顺序重建部门与成员。

### 4. PWA 离线
- `sw.js`：静态资源与 `navigate` 均**网络优先**，缓存兜底；版本号 v3。
- 注册后监听 `controllerchange` 自动 `location.reload()`，规避旧版本。

### 5. 双击空白返回（手势识别）
- 挂在 `#screen` 捕获阶段：两次 `pointerup` 落在非交互空白区，且间隔 ≤320ms、间距 ≤34px、单次位移 ≤12px、按压 ≤250ms。
- 排除 `[data-act]`、button/a/input/label/.modal-mask/.drag-handle 等交互元素。
- 多指触控期间作废；搜索框聚焦时不触发；模态框在独立 `#modalLayer` 子树，事件不冒泡到 `#screen`，天然隔离。

### 6. 拨号确认
- `confirmCall(number)` → `openModal` 显示号码 + 确认 → `doCall` 执行 `tel:`。

---

## 四、当前部署

- CloudStudio 发布地址（每次重新部署会生成新沙箱链接，旧链接失效）：
  `https://f34169afd59247fea37dbc47b2c7f42c.bj10.agentos-app.net`
- 可在「设置 - 数据管理 - 我发布的应用」中管理（删除 / 重新发布）。

---

## 五、后续可优化方向（可选）
- 单匹配自动跳转首条、搜索结果内展示电话直拨。
- 增加「检查更新」按钮，主动触发 SW 刷新，规避缓存感知延迟。
- 双击返回加入轻微视觉反馈（如水波纹）以提升可发现性（当前依赖页面离场动画反馈）。
